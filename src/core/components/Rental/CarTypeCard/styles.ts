import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    marginRight: 15,
    alignItems: 'center',
  },

  image: {
    width: 110,
    height: 110,
    borderRadius: 12,
  },

  label: {
    marginTop: 6,
    fontWeight: '600',
  },
});
