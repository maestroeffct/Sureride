export type ThemeColors = {
  background: string;
  surface: string;
  card: string;
  accent: string;
  accentMuted: string;
  textPrimary: string;
  textSecondary: string;
  border: string;
  warning: string;
  success: string;
  primaryColor: string;
};
